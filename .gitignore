# Dependencies
node_modules/

# Build output
dist/
build/

# Ortam değişkenleri — gerçek .env repoya KOYULMAZ. Şablon olarak .env.example
# izlenir; kurulumda `cp .env.example .env` ile kopyalanır. Kişisel/gizli
# override'lar .env.local içine yazılır (o da git'e dahil değildir).
.env
.env.local
*.local

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
.DS_Store

# Prisma
backend/prisma/*.db

# Docker volume mounts (local)
pgdata/

# Local file uploads (served from a Docker volume in prod)
uploads/

# TLS sertifikaları — private key repoya KOYULMAZ (scripts/gen-certs.sh üretir)
certs/
