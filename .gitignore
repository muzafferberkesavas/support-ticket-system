# Dependencies
node_modules/

# Build output
dist/
build/

# Env — the committed .env holds safe development defaults so the project
# runs straight after cloning. Keep personal overrides out of git:
.env.local
*.local

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
.DS_Store

# Prisma
backend/prisma/*.db

# Docker volume mounts (local)
pgdata/

# Local file uploads (served from a Docker volume in prod)
uploads/
